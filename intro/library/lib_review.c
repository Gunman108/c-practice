#include <stddef.h> //special constants
//NULL
//offsetof(structure, member) - the offset in bytes of the member 'member' from the start of structure 'structure'
//ptrdiff_t - type of int produced by subtracting two pointers
//size_t - type of int producted by the sizeof operator
//wchar_t - type of int required to hold a wide char

#include <limits.h> //mins and maxes for most datatypes

#include <stdbool.h> //bool, true, and false defined

#include <string.h>
//strcat() - concatenates two strings
//strchr() - looks for instances of chars
//strstr() - looks for instances of substrings
//strcmp() - compares strings
//strcpy() - assigns string values
//strlen() - returns the number of characters
//strtok() - breaks a string into tokens based on delimiter

#include <ctype.h>
//isalnum, isalpha, isblank, iscntrl, isdigit, isgraph, islower, isspace, ispunct, isupper, isxdigit
//tolower()
//toupper()

#include <stdio.h>
//EOF, NULL, stdin, stdout, stderr, FILE
//fclose() - closes file
//feof() - returns nonzero at end of file
//fflush() - flushes data from internal buffers
//fgetc(), fgetpos(), fgets(), fopen(), fprintf()
//fputc(), fputs(), fscanf()
//fseek(), fsetpos(), ftell()
//printf(), scanf()
//remove(), rename()

#include <stdlib.h>
//conversions: atof(), atoi(), atol(), atoll()
//dynamic memory allocation: calloc(), malloc(), reaalloc(), free()

int main(){
    
    return 0;
}